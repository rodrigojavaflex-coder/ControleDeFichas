{
  "name": "controle-de-fichas",
  "version": "1.0.0",
  "description": "Sistema de Controle de Fichas - Full Stack NestJS + Angular",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm run start:prod",
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build-frontend": "cd frontend && npm run build -- --configuration production",
    "build-backend": "cd backend && npm run build",
    "validate:backend": "cd backend && npm run lint && npm run test",
    "validate:frontend": "cd frontend && npm run build",
    "validate": "npm run validate:backend && npm run validate:frontend"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "author": "Rodrigo Teixeira",
  "license": "MIT"
}
